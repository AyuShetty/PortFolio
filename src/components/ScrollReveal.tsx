"use client";

// This component is intentionally minimal.
// Homepage content-section reveal is handled by PortfolioExperience's own observer.
// Subpage panels no longer need JS reveal — they render visibly by default.
export function ScrollReveal() {
  return null;
}
